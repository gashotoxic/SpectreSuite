// narrativeInjection.ts
// Reason: Implement a function to inject narrative into text

export function narrativeInjection(input: string, narrative: string): string {
    return `${narrative} ${input}`;
}